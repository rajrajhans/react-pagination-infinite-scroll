import React from "react";
import "./Loading.css";

const Loading = () => {
  return (
    <div className={"loading-container"}>
      <div>Loading ...</div>
    </div>
  );
};

export default Loading;
